// #include <iostream>
// #include <vector>
// #include <algorithm>
// 
// 
// int main(){
	// std::vector<std::vector<int>> v {};
	// std::vector<int> arr {0,0,0};
	// std::sort(arr.begin(), arr.end());
// 
	// // arr len >= 3
	// int curr = 0;
	// int l = curr ;
	// int r = arr.size() - 1;
// 
	// while (l < r){
		// std::vector<int> tmp;
		// int res = arr[curr] + arr[l] + arr[r];
// 
		// if (res == 0)
			// v.push_back({arr[curr], arr[l], arr[r]});
// 
		// while (res > 0 && (l < r)){
			// r--;
			// int res = arr[curr] + arr[l] + arr[r];
			// if (res == 0)
				// v.push_back({arr[curr], arr[l], arr[r]});
		// }
		// curr++;
		// // adjust l
		// l = curr + 1;
		// // reset r
		// r = arr.size() - 1;
	// }
	// for(auto i : v){
		// for(auto j : i){
			// std::cout << j << " ";
		// }
		// std::cout << std::endl;
	// }
// }
